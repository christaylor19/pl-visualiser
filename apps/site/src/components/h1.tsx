import styled from 'styled-components';

const H1 = styled.h1`
  color: ${({ theme }) => theme.textColour};
  font-size: 1.75rem;
  margin: 0;
  margin-bottom: 1rem;
  margin-top: 2rem;
`;

export default H1;
